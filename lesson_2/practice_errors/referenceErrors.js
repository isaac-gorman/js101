// Q: When do reference errors occur?
// A: A ReferenceError happens when a variable ...
// ... or function is invoked without it actually existing
// ... reference errors occur when a non-existing values are called

// a; // ReferenceError: a is not defined
a(); // ReferenceError: a is not defined
